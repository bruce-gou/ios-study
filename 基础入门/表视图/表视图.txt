
 在ios中，表视图是最重要的视图。
	表头视图(table header view)。表视图最上边的视图，用于展示表视图的信息。
	表脚视图(table footer view)。表视图最下边的视图，用于展示表视图
	单元格(cell)。它是组成表视图每一行的单位视图。
    节(section)。它由多个单元格组成，有节头(section header) 和节脚(section footer)。
	节头。节的头，描述节的信息。
	节脚。节的脚，也可描述节的信息和声明.
	如图 image/表视图1-1.png


    表视图分类
	普通表视图。主要用于动态表，动态表一般在单元格数目未知情况下使用。
	分组表视图。可以用于动态表和静态表。动态表分组时，单元格分成不同部分，而每一部分中单元格中的数据是类似的。
		  静态表分组时，会将功能类似的视图放置在一起。

	
	表视图样式有2种，是在枚举类型 UITableViewStle 中定义的：
		plain	普通表视图
		grouped 分组表视图







	如图见 image/表视图1-2.png



	此外，在表视图中还可以带有索引列、选择列和搜索栏等。 如图： image/表视图1-3.png. image/表视图1-4.png

	
    单元格的组成和样式  image/表视图1-5.png  表视图1-6.png 表视图1-7.png
	单元格可以有很多样式，我们可以根据需要进行选择。图标、主标题和副标题可以有选择的设置，扩展视图可以内置或者自定义，其中内置的扩展视图是在枚举类型 UITableViewCellAccessoryType 中定义的。
	如图 ：image/表视图1-5.png

	noe	没有扩展图标
	disclosureIndicator	扩展指示图标，触摸该图标将切换到下一级表视图
	detailDiscloureButton	细节展示按钮，触摸该单元格的时候，表视图会以视图的方式展示当前单元格的更多详细信息
	checkmark	选择标志，表示该行被选中
	detailButton	细节按钮，触摸该单元格的时候，会显示当前单元格的附加信息


	在开发过程中，我们应该首先考虑苹果公司提供的一些固有的单元格样式， ios API 提供的单元格样式是在枚举类型 UITableViewCellStyle 中定义的， 如图 表视图1-6.png 表视图1-7.png

	default		默认样式，只有图标和主标题
	subtitle	Subtitle 样式，有图标、主标题、副标题，副标题在主标题下面
	value1		Value1 样式，有主标题和副标题，可以有图标
	value2		Value2 样式，有主标题和副标题，无图标


    数据源协议与委托协议
	数据协议： 表视图1-8.png

	委托协议： 表视图1-9.png


    添加搜索栏
	当表视图中的数据量比较大的时候，要找到指定的数据并不是轻而易举的事情，ios 给我提供了一个搜索栏控件(UISearchBar)。


  
    分节表视图
	简单的表视图只有一节(section)，它实际上是分节表视图的一个特例，一个表可以有多个节，节也有头有脚，分节式添加索引和分组的前提。
	

      添加索引 --- 分节是添加索引的前提
	当表视图中有大量数据集合时，除了添加搜索栏，还可以通过添加索引来辅助查询。
	1.索引标题不能与显示的标题完全一样。
	2.索引标题应具有代表性
	3.如果采用索引列表视图，一般情况下就不再使用扩展视图。



    插入和删除单元格
	对于表视图，我们不仅需要浏览数据，有时还需要删除、插入和移动单元格等。
	表视图一旦进入插入和删除状态，单元格的左边就会出现一个“编辑控件”



    移动单元格
	在表视图中，单元格的顺序可以重新排列，移动单元格与插入和删除单元格类似，单元格的后面会有重排序控件



    分页模式
	1.下拉刷新模式 ：控件 UIRefreshControl
	
	











	
